# Отчёт реализации v0.5.35 — корневая структура Git

## Цель

Убрать устаревший дополнительный каталог проекта и сделать корень Git-репозитория
одновременно корнем Android-проекта.

## Итог

Перенос выполнен. Теперь после переключения на ветку `v0.5.35-root-layout`
дополнительный переход в `iRetail_Android_UI_v0.5.8_smartskypos_probe_source` не нужен.

Рабочая структура начинается непосредственно с:

- `.github/`
- `app/`
- `docs/`
- `tools/`
- `test_reports/`
- `kozenBridge/`
- `kozenRecoveryBridge/`
- `jl22AoaProbe/`
- `jl22AoaRecovery/`
- `settings.gradle`
- `build.gradle`
- `BUILD_WINDOWS_CLI.bat`

## Проверка потерь

Проведено сравнение Git-деревьев до и после переноса по SHA blob-файлов.

- исходных blob-файлов внутри старого каталога: **401**;
- отсутствующих после переноса: только намеренно заменённый
  `tools/verify_v0_5_34_s1.py`;
- непредусмотренных потерь: **0**;
- новые проверки: **2**.

Таким образом, перенос выполнен как механическое перемещение с ограниченными,
явно перечисленными изменениями версии и инфраструктуры.

## GitHub Actions

Всего в `.github/workflows/` — 30 рабочих процессов.

- 29 процессов содержали старый вложенный путь и были исправлены;
- `vendotek-wiki-study.yml` такого пути не содержал и смысловой правки не требовал;
- основной Android build теперь запускается из корня.

## Проверки

На коммите `a5537b621460af057334eda5a33cddd422bd984e`:

- root-layout guard — **SUCCESS**;
- S1 truthful-state guard — **SUCCESS**;
- Android `assembleDebug` — **SUCCESS**;
- загрузка APK в GitHub Actions — **SUCCESS**.

## Прогресс

### Выпуск 0.5.35
- всего шагов: **6**
- выполнено: **6**
- осталось: **0**
- программная готовность структурного выпуска: **100%**

### Основной контракт S0–S6
Перенос структуры не закрывает новый функциональный этап.
Закрыты S0 и S1: **2 из 7 рубежей = 29% по числу рубежей**.

Следующий содержательный этап — S2: каталог и денежная точность.
