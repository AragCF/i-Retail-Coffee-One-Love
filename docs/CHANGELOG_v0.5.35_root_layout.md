# CHANGELOG v0.5.35 — перенос проекта в корень репозитория

Дата: 20.09.2026  
Основа: `v0.5.34-s1-truthful-states`  
Назначение выпуска: структурное, без изменения бизнес-логики S1.

## 🟢 Добавлено

- Корневая структура Android-проекта: `app/`, `docs/`, `tools/`, `test_reports/`,
  `kozenBridge/`, `kozenRecoveryBridge/`, `jl22AoaProbe/`, `jl22AoaRecovery/`.
- Проверка `tools/verify_v0_5_35_root_layout.py`.
- Версионно-независимая проверка S1 `tools/verify_s1_truthful_states.py`.
- Актуальный корневой `README.md`.

## 🟡 Изменено

- `versionCode`: 34 → 35.
- `versionName`: `0.5.34-s1-truthful-states` → `0.5.35-root-layout`.
- `BUILD_WINDOWS_CLI.bat` теперь работает непосредственно из корня репозитория.
- 29 файлов GitHub Actions, содержавших старый вложенный путь, переведены на корневую структуру.
- Имя сборочного артефакта: `iRetail-v0.5.35-debug-apk`.

## 🔴 Удалено

- Лишний уровень каталога `iRetail_Android_UI_v0.5.8_smartskypos_probe_source/`.
- Старый привязанный к номеру версии сценарий `tools/verify_v0_5_34_s1.py`;
  его инварианты перенесены в `verify_s1_truthful_states.py`.

## ⚠️ Что не менялось

- Wire-протокол SmartSkyPOS/AOA.
- Wire-протокол VTK/Vendotek.
- Последовательности финансовых команд.
- URL и имена полей I-Retail.
- Каталог, лояльность и денежная модель.
- Логика S1 по честным состояниям оплаты/фискализации/выдачи.
- Multicard по-прежнему не входит в текущий этап.

## Проверка целостности переноса

Перед переносом внутри вложенного проекта было **401 blob-файл**.
После переноса в корне сохранены все исходные пути, кроме одного намеренно удалённого
`tools/verify_v0_5_34_s1.py`.

Фактические содержательные изменения относительно v0.5.34 до добавления новых проверок:

- `BUILD_WINDOWS_CLI.bat`;
- `README.md`;
- `app/build.gradle`;
- `MainActivity.kt`.

Добавлены два новых проверочных сценария. Потерянных исходников при переносе не обнаружено.

## Автоматическая проверка

Для коммита `a5537b621460af057334eda5a33cddd422bd984e` успешно выполнены:

1. проверка корневой структуры;
2. проверка инвариантов S1;
3. `assembleDebug`;
4. публикация APK-артефакта.

Артефакт GitHub Actions: `iRetail-v0.5.35-debug-apk`.  
SHA-256 ZIP-артефакта GitHub Actions: `64b9c16eb0c8be97cb2ff2c4059218972c36ba89f3e0daa88ab7c3bb7e6e8518`.
