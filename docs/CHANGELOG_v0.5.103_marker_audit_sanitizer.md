# CHANGELOG v0.5.103 — marker audit sanitizer

Дата: 24.09.2026.

Живой read-only аудит v0.5.102 дошёл до анализа, но safety scan отклонил собственный отчёт:
- длинный числовой идентификатор остался в `07_fiscalization_dry_run.txt`;
- длинный числовой идентификатор остался в `SUMMARY.txt`.

Исправлено:
- SUMMARY пропускается через общий Redact до записи;
- fiscalization_dry_run преобразуется в `07_fiscalization_dry_run_safe.txt`;
- исходный fiscalization_dry_run удаляется до safety scan;
- добавлен исполняемый PowerShell-fixture с длинными идентификаторами и псевдо-TID;
- CI проверяет, что raw-файлы удалены, длинные числовые токены не переживают очистку и результат аудита сохраняется.

Финансовые команды не добавлялись. Маркер и платёжное состояние не удаляются.