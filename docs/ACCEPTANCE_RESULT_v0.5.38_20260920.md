# Результат живой приёмки v0.5.38 — 20.09.2026

Источник доказательств: `test_reports/s23_acceptance/S23_ACCEPTANCE_20260920_080716.zip`.

## Итог

### S2 — живой каталог I-Retail

**Не принят.**

Фактический журнал:
- `success=false`;
- `source=content XML`;
- `products=0`;
- `offers=0`;
- `categories=0`.

То есть приложение не получило подтверждённый живой ZIP-каталог I-Retail и осталось на локальном XML-содержимом.

Причина отказа в текущем пакете доказательств не записана: прежний журнал фиксировал состояние и источник, но не безопасный этап/код ошибки. Это будет исправлено в следующем диагностическом выпуске.

### S3 — безопасный черновик заказа

**Принят для подстадии DRY_RUN.**

Подтверждено:
- `mode=DRY_RUN_ONLY`;
- `send_allowed=false`;
- `lines_equal_gross=true`;
- сформирован свежий черновик после предварительного удаления старого;
- один товар;
- количество 1;
- цена строки 149.00;
- сумма строк 149.00;
- общая сумма 149.00;
- к оплате 149.00;
- скидка 0.00;
- присутствуют channel/device/currency поля;
- неизвестные `id`, `employee_id`, `pin` оставлены `null`;
- сетевой `iretail/order/synchronize` не включён.

## Установленная версия

- `versionCode=38`;
- `versionName=0.5.38-s2-s3-acceptance`;
- Android-пакет установлен и запущен на JL22.

## Проверенное устройство

JL22 подтверждается по полной сигнатуре ADB:

- `product:octopus_jetinno`;
- `model:UniWin_M190`;
- `device:octopus-jetinno`.

В дальнейших испытательных сценариях устройство определяется по этой сигнатуре автоматически. Если однозначно определить JL22 невозможно, сценарий обязан показать нумерованный список найденных Android-устройств и запросить выбор человека.

## Следующий шаг

1. Зафиксировать безопасную диагностику этапа отказа каталога: authentication / download / parse / cache.
2. Не выводить пароли, access token, client secret и банковские данные.
3. Повторить живое обновление каталога.
4. Только после доказанного `source=I-Retail ZIP` закрывать S2.
5. Серверную отправку заказа S3 пока не включать.

## Прогресс

- S2: **5/6, 83%** — остаётся живой каталог.
- S3 DRY_RUN: **4/4, 100%** — подстадия закрыта.
- Общий контракт S0–S6: полностью закрыты S0 и S1; S2 ещё не закрыт.
